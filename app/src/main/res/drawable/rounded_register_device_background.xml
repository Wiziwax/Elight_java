<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/rounded_button.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Button background color -->
    <stroke
        android:width="1dp"
        android:color="@color/outline_color" /> <!-- Outline color -->
    <corners android:radius="16dp" /> <!-- Corner radius for rounded edges -->
    <padding
        android:left="20dp"
        android:top="20dp"
        android:right="20dp"
        android:bottom="20dp" />
</shape>